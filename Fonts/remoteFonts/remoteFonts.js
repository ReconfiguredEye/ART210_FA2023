function setup() {
  createCanvas(400, 200);
  background(0);
}

function draw() {
  // Using the Google Font - just specify the name
  textFont('Pixelify Sans');
  textSize(48);
  fill(255, 100, 100);
  text('Hello, Google Font!', 10, 100);
}
